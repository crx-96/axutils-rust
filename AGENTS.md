# axutils 项目协作规则

本项目是单一 Rust library crate `axutils`，入口为 `src/lib.rs`，默认 feature 为空。
本文件维护协作方式、任务入口与仓库边界；库级设计判断放在项目 Skill，模块映射和验证命令各自维护。

工程定位是可被不同应用组合使用的通用 Rust library。设计和审查优先保障下游公共契约、能力隔离、
资源生命周期与跨平台行为；进程级 runtime、日志订阅器及真实服务配置由调用方显式决定。

## 协作与判断

- 说明、审查意见、提交信息、注释和文档默认使用中文，标识符、API 和标准技术术语保留原文。
- 从请求和已有上下文确定目标，在授权范围内持续完成实施、验证和交付。常规、可逆选择依据
  项目惯例处理；后续消息用于修正当前工作，保留仍有效的目标、约束和已完成成果。
- 分析深度随不确定性、影响范围和失败代价调整。简单且定位清楚的任务直接处理；复杂任务先
  理解相关架构、调用与状态流、关键假设、边界和失败路径，用可观察结果检验方案。把精力投入
  会改变决定的疑点，证据足以支持行动时推进，避免无新增信息的反复推演。
- 优先复用现有实现，选择兼顾正确性、可维护性和实际效果的最小充分方案。额外抽象、依赖、
  重构和验证以当前问题的收益为依据；减少重复步骤的同时保留关键分析、回归和安全验证。
- 当缺失信息无法从上下文取舍，且会实质改变范围、公共行为、接口、数据、成本、风险或验收时
  再询问。已有授权继续适用；需要答案时暂缓依赖它的动作，继续独立工作并准备可审阅结果。
  普通实现选择、文档建议和流程调整可自行处理，不把它们扩展为审批环节。
- 用户明确要求优先于项目工作偏好；适用的权限和安全边界由运行环境执行。若规则或工具实际
  阻塞工作，说明具体依据和未完成部分，继续推进范围内可行的替代路径。
- 补充规范沿用已有约定，只填补缺口，主要约束新增内容及本次确需修改的行为。旧内容与新规范
  的差距在审查中单独说明，不因此批量重构、变更公共 API 或升级依赖；确需替换现有规则时，
  先说明冲突、理由和迁移影响，再按用户选择处理。

## 按需入口

优先利用已加载且仍有效的信息，按当前判断需要查阅相关章节；发现跨章节关系或上下文不足时
再扩大阅读。链接是定位入口，不代表每次任务都要逐份加载或通读。

- **库级变更或判断**：源码实现、公共 API、错误/运行时/安全语义、feature/依赖、发布元数据，
  以及测试、fixture、用户文档与这些契约的一致性，可查阅
  [`review-rust-library-change`](docs/skills/review-rust-library-change/SKILL.md) 对应主题。
  用户要求按本项目 Rust library 标准审查时，以其中适用的设计与验收依据开展工作。
  规范检查区分已确认违反现有契约、与新增规范的差距、改进建议和未验证项；问题附规则位置、
  代码位置、触发条件、影响及已有验证结果，不能把单一构建通过等同于全库合规。
- **纯维护**：翻译、措辞、格式、规则/Skill、CI/开发工具、Git 和不判断库契约的测试基础设施维护，
  直接处理相关文件。维护规则或 Skill 时核对受影响的原意、上下文和引用；后续涉及库级判断时
  再查阅对应主题。
- **模块定位**：工具类、领域模块、公共路径、feature 或跨模块 API 的归属见
  [`docs/module-map.md`](docs/module-map.md)。
- **开发与验证**：命令、验证范围、失败处理和 live 测试运行条件见
  [`docs/develop.md`](docs/develop.md)。纯规则、措辞和格式维护通常检查文档结构、链接及 diff 即可。

## 仓库边界与交付

- `config/` 存放本地测试配置；密码、授权码、邮箱、节点地址等敏感内容留在被忽略的本地配置中，
  提交、命令行、日志、测试输出和文档使用占位符及脱敏信息。
- 当前版本、edition 和 MSRV 以 `Cargo.toml` 为准；涉及这些信息或 CHANGELOG 判断时核对 manifest。
  用户可见契约变化的记录范围见 Skill；规则、CI、开发工具、Skill 和纯文档整理通常不写 CHANGELOG。
- `package.include` 管理发布白名单，具体清单见 develop 的发布前检查；根目录 `Cargo.lock`
  不作为 library 依赖版本策略提交。
- 改版本、切换工具链、修改全局环境、发布、推送和连接真实外部服务按具体目标与已有授权处理；
  普通代码或文档修改请求不隐含这些外部操作的授权。
- 交付说明实际变更、验证范围与结果、未运行项和剩余问题，区分成功、预期失败与环境阻塞。
  结论对应已有证据，局部检查按局部结果报告；篇幅按任务需要安排。
